# Service icons

The Umbrel Arr service icons are the canonical SVG assets used by the sibling
`umbrel-arr-app-store` packages. Jellyfin and Plex use their Simple Icons v16
SVG marks. These files are presentation assets only; service state and health
still come from umbrelarr's API-only reconciler.
